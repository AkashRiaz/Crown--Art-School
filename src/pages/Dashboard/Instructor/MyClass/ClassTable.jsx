import React from "react";

const ClassTable = ({ cls }) => {
  console.log(cls);
  const { photo, className, instructorName, availableSeats, email,price } = cls;
  return (
    <div>
     
          
            {/* row 1 */}
            
        
    </div>
  );
};

export default ClassTable;
